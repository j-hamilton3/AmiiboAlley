<h1>Review Your Order</h1>

<h2> Customer Details</h2>

<ul>
  <li>Name: <%= current_user.name %></li>
  <li>Province: <%= @userProvince.provinceName %></li>
  <li>Address: <%= current_user.address %></li>
  <li>Postal Code: <%= current_user.postalCode %> </li>
</ul>

<h2> Amiibos Ordered </h2>

<ul>
  <% @amiibos_with_quantities.each do |item| %>
    <li><%= item[:amiibo].name %> - $<%= item[:amiibo].price %> x <%= item[:quantity] %> = $<%= item[:amiibo].price * item[:quantity] %></li>
  <% end %>
</ul>

<h2> Subtotal </h2>
<p>$<%= @subtotal %></p>

<h2> Taxes Charged (GST + PST + HST) </h2>
<p>$<%= @subtotal %> * <%= @userProvince.taxRate %> = $<%= @totalTaxes%> </p>

<h2> Order Total </h2>
<p>$<%= @total %></p>

<%= button_to 'Proceed to Checkout', checkout_create_path, method: :post, params: { quantities: params[:quantities] }, class: "button is-primary" %>
